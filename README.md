# piano_accuracy_algorithm
This algorithm uses a step-wise dynamic time warp with a Gaussian pyramid to compare a piano practice session with an ideal recording.

## Enviroment setup
```bash
python -m venv .venv #create virtual enviroment
source .venv/bin/activate #activate env
pip install -r requirements.txt
```

